import React from 'react';
import styles from './CheckboxComponent.module.sass';

const CheckboxComponent = () => {
   return (
      <div className={styles.checkboxWrapper19}>
         <input type='checkbox' id='cbtest-19' />
         <label htmlFor='cbtest-19' className={styles.checkBox}></label>
      </div>
   );
};

export default CheckboxComponent;
